#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@ --with python3 --buildsystem=pybuild
	go build autoISO.go
	strip autoISO

override_dh_auto_test:
	./tests/test_black
	./tests/test_flake8
	./tests/test_shellcheck
